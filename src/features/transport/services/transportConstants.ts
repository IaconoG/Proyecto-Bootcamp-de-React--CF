import { Transport } from "../types/transport.types";

export const INITIAL_TRANSPORT: Transport = {
  title: "Transport",
  data: {
    transport: [],
  },
};

export const LOCAL_STORAGE_TRANSPORT = "transportWidget";
